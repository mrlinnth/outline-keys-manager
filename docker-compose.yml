services:
  app:
    image: outline-key-manager:local
    build: .
    pull_policy: build
    container_name: outline-key-manager
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
